# env variables can be set in IntelliJ: 'Run/Debug Configurations'
spring:
  datasource:
    driver-class-name: "org.postgresql.Driver"
    url: "jdbc:postgresql://localhost:5432/moviedb"
    username: ${PG_USER}
    password: ${PG_PASSWORD}
